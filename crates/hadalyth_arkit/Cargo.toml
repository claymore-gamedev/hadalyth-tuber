[package]
name = "hadalyth-arkit"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
godot = {workspace = true}
tokio = {workspace = true, features=["full"]}
byteorder = {workspace = true}